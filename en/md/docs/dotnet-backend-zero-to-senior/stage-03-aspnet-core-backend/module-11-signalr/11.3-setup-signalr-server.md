# 11.3 — 3. Setup SignalR Server

> Nguồn: https://tiennhm.io.vn/en/docs/dotnet-backend-zero-to-senior/stage-03-aspnet-core-backend/module-11-signalr/11.3-setup-signalr-server
> Strongly-typed Hub, xử lý lỗi trong Hub method, HubFilter, MessagePack, và những giới hạn cấu hình cần đặt trước khi lên production.

> Bốn quyết định khi dựng Hub, và cái đầu tiên là dễ nhất nhưng hay bị bỏ qua: **`Hub` với interface** khiến `Clients.User(id).ReceiveNotification(dto)` được **kiểm tra lúc biên dịch** — không có nó, gõ sai tên hàm client chỉ là một tin nhắn **rơi vào hư không**, không lỗi, không cảnh báo. Thứ hai: **exception trong Hub method không giống trong controller** — mặc định client chỉ nhận một thông điệp chung, và bật `EnableDetailedErrors` ở production là gửi **stack trace cho client**. Thứ ba: **`HubFilter`** là nơi đặt logging, xử lý lỗi và rate limit tập trung. Thứ tư: **MessagePack** giảm kích thước tin nhắn đáng kể khi lưu lượng lớn.

## Mục tiêu bài học

Sau bài này bạn có thể:

- Viết strongly-typed Hub và hiểu nó bảo vệ điều gì.
- Xử lý exception trong Hub method đúng cách.
- Viết `HubFilter` cho mối quan tâm cắt ngang.
- Đặt các giới hạn cấu hình quan trọng.
- Chọn giữa JSON và MessagePack.

## Nội dung bài học

### 11.4.1 — Strongly-typed Hub

```csharp
// Nhung gi SERVER goi tren CLIENT
public interface INotificationClient
{
    Task ReceiveNotification(NotificationDto notification);
    Task ReceiveLeadUpdate(LeadUpdateDto update);
    Task UserPresenceChanged(string userId, bool isOnline);
}

public sealed class NotificationHub : Hub<INotificationClient>
{
    // Nhung gi CLIENT goi tren SERVER — la method public cua Hub
    public Task Ping() => Clients.Caller.ReceiveNotification(
        new NotificationDto(Guid.NewGuid(), "pong", NotificationType.Info, DateTime.UtcNow));
}
```

So sánh:

```csharp
// KHONG strongly-typed — chuoi ma thuat
await Clients.User(id).SendAsync("ReceiveNotifcation", dto);   // go sai -> im lang

// Strongly-typed — kiem tra luc bien dich
await Clients.User(id).ReceiveNotification(dto);
```

Dòng đầu **biên dịch được**, chạy được, và tin nhắn **không bao giờ tới client** — vì client đăng ký `"ReceiveNotification"` chứ không phải `"ReceiveNotifcation"`. Không có exception, không có log. Bạn chỉ biết khi người dùng báo "tôi không nhận được thông báo".

Đây là loại lỗi tốn nhiều giờ nhất trong SignalR, và interface xoá bỏ nó hoàn toàn.

Đặt interface vào một project **chia sẻ** thì client .NET (Blazor, MAUI) dùng lại được. Với client TypeScript, sinh kiểu từ interface đó bằng một công cụ như NSwag.

### 11.4.2 — Exception trong Hub method

```csharp
public async Task MarkAsRead(Guid notificationId)
{
    var notification = await _repo.GetAsync(notificationId);
    if (notification is null)
        throw new NotFoundException(...);       // client nhan gi?
}
```

Khác với controller, ở đây **không có `IExceptionHandler`**, không có `ProblemDetails`, không có mã trạng thái. Mặc định client nhận:

```
An unexpected error occurred invoking 'MarkAsRead' on the server.
```

Không có chi tiết — đó là **mặc định đúng**, vì chi tiết sẽ lộ thông tin nội bộ.

Muốn client biết lý do thì ném `HubException`, thứ duy nhất được gửi nguyên văn:

```csharp
public async Task MarkAsRead(Guid notificationId)
{
    var userId = Context.UserIdentifier
        ?? throw new HubException("Chưa xác thực");

    await using var scope = _scopeFactory.CreateAsyncScope();
    var repo = scope.ServiceProvider.GetRequiredService<INotificationRepository>();

    var notification = await repo.GetAsync(notificationId, Context.ConnectionAborted);

    if (notification is null || notification.UserId != userId)
        throw new HubException("Không tìm thấy thông báo");   // KHONG lo ton tai hay khong

    await repo.MarkAsReadAsync(notificationId, Context.ConnectionAborted);
}
```

Ba chi tiết:

**`HubException` là kênh duy nhất** để gửi thông điệp có ý nghĩa cho client. Mọi exception khác bị che đi.

**`Context.ConnectionAborted`** là `CancellationToken` của kết nối — truyền nó xuống mọi lời gọi async để công việc dừng khi client ngắt ([bài 6.5](../../stage-02-csharp-professional/module-06-async-programming/6.4-cancellationtoken.mdx)).

**Cùng một thông điệp** cho "không tồn tại" và "không thuộc về bạn" — tránh lộ thông tin, đúng như IDOR ở [bài 10.7](../module-10-authentication-authorization/10.6-resource-based-authorization.mdx). Phân quyền trong Hub cũng dễ bị bỏ sót y như trong controller.

`EnableDetailedErrors = true` gửi **toàn bộ** thông điệp exception cho client — chỉ bật ở Development.

### 11.4.3 — `HubFilter`

```csharp
public sealed class LoggingHubFilter(ILogger<LoggingHubFilter> logger) : IHubFilter
{
    public async ValueTask<object?> InvokeMethodAsync(
        HubInvocationContext context,
        Func<HubInvocationContext, ValueTask<object?>> next)
    {
        var start = Stopwatch.GetTimestamp();

        try
        {
            return await next(context);
        }
        catch (HubException)
        {
            throw;                                   // loi nghiep vu — de nguyen
        }
        catch (Exception ex)
        {
            logger.LogError(ex, "Loi trong {Hub}.{Method} cua {User}",
                context.Hub.GetType().Name,
                context.HubMethodName,
                context.Context.UserIdentifier);

            throw new HubException("Đã xảy ra lỗi, vui lòng thử lại");   // che chi tiet
        }
        finally
        {
            logger.LogInformation("{Method} mat {Ms}ms",
                context.HubMethodName,
                Stopwatch.GetElapsedTime(start).TotalMilliseconds);
        }
    }

    public Task OnConnectedAsync(HubLifetimeContext context, Func<HubLifetimeContext, Task> next)
    {
        logger.LogInformation("{User} ket noi ({ConnectionId})",
            context.Context.UserIdentifier, context.Context.ConnectionId);
        return next(context);
    }

    public Task OnDisconnectedAsync(
        HubLifetimeContext context, Exception? exception, Func<HubLifetimeContext, Exception?, Task> next)
        => next(context, exception);
}
```

```csharp
builder.Services.AddSignalR(options =>
{
    options.AddFilter<LoggingHubFilter>();
})
.AddHubOptions<NotificationHub>(options =>
{
    options.AddFilter<RateLimitHubFilter>();      // chi cho Hub nay
});
```

`HubFilter` là tương đương của middleware cho Hub — cùng vai trò như `IExceptionHandler` trong controller ([bài 8.9](../module-08-aspnet-core-fundamentals/8.8-global-exception-handling.mdx)).

Mẫu bắt lỗi ở trên rất đáng dùng: **log đầy đủ ở server, gửi thông điệp chung cho client**, đúng nguyên tắc bảo mật, và bạn không phải lặp `try/catch` trong từng Hub method.

Một ứng dụng khác của filter: **rate limit cho Hub method** — `[EnableRateLimiting]` **không** áp dụng cho lời gọi Hub, chỉ cho request HTTP. Client gọi một Hub method trong vòng lặp thì không có gì chặn.

### 11.4.4 — Cấu hình

```csharp
builder.Services.AddSignalR(options =>
{
    options.EnableDetailedErrors      = builder.Environment.IsDevelopment();
    options.KeepAliveInterval         = TimeSpan.FromSeconds(15);
    options.ClientTimeoutInterval     = TimeSpan.FromSeconds(30);
    options.HandshakeTimeout          = TimeSpan.FromSeconds(15);
    options.MaximumReceiveMessageSize = 32 * 1024;
    options.StreamBufferCapacity      = 10;
    options.MaximumParallelInvocationsPerClient = 1;
});

app.MapHub<NotificationHub>("/hubs/notifications")
   .RequireAuthorization();
```

| Tuỳ chọn | Ý nghĩa |
|---|---|
| `MaximumReceiveMessageSize` | Trần tin nhắn từ client. 32KB mặc định — đủ cho tin nhắn, không dành cho file |
| `MaximumParallelInvocationsPerClient` | Mặc định **1**: một client gửi 10 lời gọi thì chúng chạy **tuần tự** |
| `StreamBufferCapacity` | Số item đệm khi stream |
| `HandshakeTimeout` | Thời gian chờ handshake sau khi transport mở |

`MaximumParallelInvocationsPerClient = 1` là mặc định đáng biết: nó ngăn một client làm quá tải server, nhưng cũng nghĩa là một Hub method **chậm** sẽ chặn mọi lời gọi khác **của chính client đó**. Nếu Hub có method mất vài giây, hãy tăng giá trị này hoặc đẩy việc ra hàng đợi.

`.RequireAuthorization()` trên `MapHub` là cách gọn nhất để bắt buộc xác thực — chi tiết ở [bài 11.6](11.5-signalr-authentication.mdx).

### 11.4.5 — MessagePack

```bash
dotnet add package Microsoft.AspNetCore.SignalR.Protocols.MessagePack
```

```csharp
builder.Services.AddSignalR().AddMessagePackProtocol();
```

```javascript
new signalR.HubConnectionBuilder()
    .withUrl("/hubs/notifications")
    .withHubProtocol(new signalR.protocols.msgpack.MessagePackHubProtocol())
    .build();
```

MessagePack là định dạng nhị phân, thường nhỏ hơn JSON **30–50%** và parse nhanh hơn. Đáng dùng khi lưu lượng tin nhắn lớn hoặc client trên mạng di động.

Đánh đổi: tin nhắn **không đọc được** trong DevTools, nên debug khó hơn đáng kể. Thực dụng nhất là **giữ JSON ở Development** và bật MessagePack ở production — SignalR hỗ trợ nhiều protocol cùng lúc, client chọn lúc kết nối.

### 11.4.6 — Rà lại code của bạn

**Danh sách rà soát Hub server**

- [ ] Hub kế thừa Hub với interface, không dùng SendAsync với chuỗi.
- [ ] Interface client nằm ở project chia sẻ nếu có client .NET.
- [ ] Chỉ ném HubException khi muốn client thấy thông điệp.
- [ ] EnableDetailedErrors chỉ bật ở Development.
- [ ] Context.ConnectionAborted được truyền xuống mọi lời gọi async.
- [ ] Mọi Hub method đều kiểm tra quyền trên dữ liệu, không chỉ dựa vào [Authorize].
- [ ] Có HubFilter bắt lỗi, log đầy đủ ở server và gửi thông điệp chung cho client.
- [ ] Có cơ chế chặn client gọi Hub method quá nhanh — attribute rate limit không áp dụng.
- [ ] MaximumReceiveMessageSize được đặt, không dùng SignalR để truyền file.
- [ ] Biết MaximumParallelInvocationsPerClient mặc định là 1.

## Bài tập áp dụng

1. **Gõ sai tên hàm.** Gọi `Clients.User(id).SendAsync("ReceiveNotifcation", dto)` với một chữ sai, xác nhận **không có lỗi nào** và client không nhận gì. Chuyển sang strongly-typed và xác nhận nó không biên dịch được.

2. **Exception bị che.** Ném `InvalidOperationException` trong một Hub method và ghi lại thông điệp client nhận. Đổi sang `HubException` và so sánh. Sau đó bật `EnableDetailedErrors` và xem client nhận được gì.

3. **Lời gọi tuần tự.** Viết một Hub method mất 3 giây, gọi nó rồi gọi ngay một method nhanh từ cùng client. Đo thời gian method thứ hai hoàn thành và giải thích.

## Tự kiểm tra

### Strongly-typed Hub bảo vệ điều gì?

Nó khiến việc gọi hàm trên client được kiểm tra lúc biên dịch. Không có nó, gõ sai tên hàm chỉ làm tin nhắn rơi vào hư không, không exception và không log, nên bạn chỉ biết khi người dùng báo không nhận được thông báo. Đây là loại lỗi tốn nhiều giờ nhất trong SignalR.

### Exception trong Hub method được xử lý thế nào?

Khác controller, không có IExceptionHandler hay ProblemDetails. Mặc định client chỉ nhận một thông điệp chung không chi tiết, và đó là mặc định đúng vì chi tiết sẽ lộ thông tin nội bộ. HubException là kênh duy nhất để gửi thông điệp có ý nghĩa cho client.

### Context.ConnectionAborted dùng để làm gì?

Nó là CancellationToken của kết nối, nên truyền nó xuống mọi lời gọi async để công việc dừng khi client ngắt kết nối. Không truyền thì server vẫn chạy tiếp một việc mà không còn ai chờ kết quả.

### HubFilter làm được gì?

Nó là tương đương của middleware cho Hub, đặt logging, đo thời gian và xử lý lỗi tập trung thay vì lặp try catch trong từng method. Một ứng dụng quan trọng khác là rate limit cho Hub method, vì attribute EnableRateLimiting chỉ áp cho request HTTP chứ không áp cho lời gọi Hub.

### MaximumParallelInvocationsPerClient mặc định là bao nhiêu và có nghĩa gì?

Mặc định là 1, nghĩa là các lời gọi từ cùng một client chạy tuần tự. Nó ngăn một client làm quá tải server, nhưng cũng nghĩa là một Hub method chậm sẽ chặn mọi lời gọi khác của chính client đó. Với method mất vài giây thì nên tăng giá trị này hoặc đẩy việc ra hàng đợi.

### Có nên dùng MessagePack không?

Nó nhỏ hơn JSON khoảng 30 tới 50 phần trăm và parse nhanh hơn, đáng dùng khi lưu lượng lớn hoặc client trên mạng di động. Đánh đổi là tin nhắn không đọc được trong DevTools nên debug khó hơn. Thực dụng nhất là giữ JSON ở Development và bật MessagePack ở production.

## Kết luận

Ba điều đáng nhớ nhất:

1. **`Hub` với interface.** Gõ sai tên hàm client là tin nhắn biến mất im lặng.
2. **Chỉ `HubException` tới được client.** Mọi exception khác bị che — và đó là điều đúng.
3. **Attribute rate limit không áp cho Hub method.** Dùng `HubFilter`.

## Tham khảo

- [Use hubs in SignalR for ASP.NET Core](https://learn.microsoft.com/en-us/aspnet/core/signalr/hubs)
- [ASP.NET Core SignalR configuration](https://learn.microsoft.com/en-us/aspnet/core/signalr/configuration)
- [Use hub filters in ASP.NET Core SignalR](https://learn.microsoft.com/en-us/aspnet/core/signalr/hub-filters)
- [Security considerations in ASP.NET Core SignalR](https://learn.microsoft.com/en-us/aspnet/core/signalr/security)

## Điều hướng

- **Bài trước:** [11.2 — 2. SignalR Architecture](11.2-signalr-architecture.mdx)
- **Bài tiếp theo:** [11.4 — 4. Client-side Integration](11.4-client-side-integration.mdx)
- **Về module:** [Trang mục lục](./)
