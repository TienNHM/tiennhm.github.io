import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tiennhm.github.io',
  appName: 'tiennhm-github-io',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
