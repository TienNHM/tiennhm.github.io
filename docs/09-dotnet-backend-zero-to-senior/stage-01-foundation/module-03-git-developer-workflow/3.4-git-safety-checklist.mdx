---
title: "3.4 — Checklist an toàn khi làm Git"
sidebar_position: 4
---

import { SummaryBox, HiddenContent } from '@site/src/components/SEO';

# 3.4 — Checklist an toàn khi làm Git

## Mục tiêu bài học

- Nắm được ý chính của bài và mối liên hệ với module.
- Áp dụng được kiến thức vào bối cảnh CRM/.NET backend.
- Sẵn sàng chuyển sang bài kế tiếp với nền tảng chắc chắn.

## Nội dung bài học

Trước mỗi lần `commit` hoặc `push`, hãy chạy checklist 30 giây:

- Mình đang đứng đúng branch chưa (`git branch`)?
- Có file rác không nên commit không (`bin/`, `obj/`, file log)?
- Commit message có nói rõ mục đích thay đổi không?
- Đã `pull` nhánh mới nhất trước khi mở PR chưa?

### 3.4.1 — Ví dụ quy trình 1 task từ đầu đến cuối

Giả sử bạn nhận task: "Thêm endpoint lấy danh sách khách hàng".

1. Tạo nhánh: `git checkout -b feature/customer-list-endpoint`
2. Code + test local.
3. Xem thay đổi: `git status` và `git diff`
4. Commit: `git commit -m "feat(api): add customer list endpoint"`
5. Push: `git push -u origin feature/customer-list-endpoint`
6. Mở PR, ghi rõ: mục tiêu, cách test, ảnh/chứng cứ nếu cần.

Lặp đúng quy trình này vài lần, bạn sẽ quen nhịp làm việc chuẩn team backend rất nhanh.

## Bài tập áp dụng

1. Tóm tắt bài học bằng ngôn ngữ của bạn.
2. Liên hệ nội dung với một tình huống thực tế trong dự án.
3. Đề xuất một cải tiến cụ thể sau khi học bài này.

## Tự kiểm tra

- Bạn có thể giải thích lại nội dung chính trong 2 phút không?
- Bạn có ví dụ áp dụng thực tế chưa?
- Bạn biết bước tiếp theo cần học/triển khai là gì không?

## Kết luận

Hoàn thành bài này giúp bạn có góc nhìn đầy đủ hơn trước khi đi tiếp trong module.

## Điều hướng

- **Bài trước:** [3.3 — 2. Cài đặt và cấu hình Git](3.3-git-install-config.mdx)
- **Bài tiếp theo:** [3.5 — 3. Workflow cơ bản](3.5-workflow-co-ban.mdx)
- **Về module:** [Trang mục lục](./)
