---
title: "8.8 — 7. Logging với Serilog"
description: "Structured logging: vì sao nội suy chuỗi phá hỏng log, message template, log level đúng nghĩa, scope, sink và những gì không bao giờ được ghi."
sidebar_position: 8
---

import { SummaryBox, Checklist, FAQSection } from '@site/src/components/SEO';

# 8.8 — 7. Logging với Serilog

<SummaryBox>
Khác biệt giữa log dùng được và log vô dụng nằm ở đúng một ký tự: dấu `$`. `LogInformation($"Customer {id} created")` tạo ra một **chuỗi đã nấu chín** — bạn chỉ tìm kiếm được bằng văn bản. `LogInformation("Customer {CustomerId} created", id)` giữ `CustomerId` thành **một trường có cấu trúc**, nên bạn truy vấn được `CustomerId = 42` trên hàng triệu dòng log. Đó là toàn bộ ý nghĩa của structured logging. Serilog thêm vào: **sink** (ghi đi đâu), **enricher** (thêm ngữ cảnh chung), và `UseSerilogRequestLogging()` — thứ thay **mười dòng log framework mỗi request** bằng **một dòng tóm tắt** có đủ thông tin.
</SummaryBox>

## Mục tiêu bài học

Sau bài này bạn có thể:

- Giải thích vì sao nội suy chuỗi phá hỏng log có cấu trúc.
- Chọn đúng log level cho từng tình huống.
- Dùng scope để gắn ngữ cảnh cho một nhóm log.
- Cấu hình Serilog từ `appsettings.json`.
- Biết chắc những gì không bao giờ được ghi vào log.

## Nội dung bài học

### 8.8.1 — Message template, không phải nội suy chuỗi

```csharp
// SAI — chuoi da nau chin, khong con truong nao
logger.LogInformation($"Customer {customer.Id} created by {userId}");

// DUNG — CustomerId va UserId la truong truy van duoc
logger.LogInformation("Customer {CustomerId} created by {UserId}",
    customer.Id, userId);
```

Dòng thứ hai sinh ra JSON:

```json
{
  "@t": "2026-09-24T10:15:00Z",
  "@mt": "Customer {CustomerId} created by {UserId}",
  "CustomerId": 42,
  "UserId": "u-77"
}
```

Bạn truy vấn được `CustomerId = 42` trong Seq, Elasticsearch hay Loki. Với bản nội suy, bạn chỉ có một chuỗi và phải tìm bằng văn bản — đúng lúc đang cần tìm nhanh nhất.

Còn hai lý do nữa:

- **Hiệu năng.** Nội suy chuỗi **luôn** chạy, kể cả khi `Debug` đang tắt. Message template chỉ nối khi log thật sự được ghi.
- **Gom nhóm.** Cùng một `@mt` là cùng một loại sự kiện, nên hệ thống log đếm được "sự kiện này xảy ra 12.000 lần hôm nay".

Hai quy ước đặt tên: **PascalCase** cho placeholder, và tiền tố `@` khi muốn Serilog ghi cả đối tượng thay vì gọi `ToString()`:

```csharp
logger.LogInformation("Nhan don hang {@Order}", order);   // ghi ca object
```

Cẩn thận với `@`: nó ghi **toàn bộ** thuộc tính, nên rất dễ đưa dữ liệu nhạy cảm vào log.

Bật analyzer để không ai quên:

```xml
<PackageReference Include="SerilogAnalyzer" Version="*" PrivateAssets="all" />
```

### 8.8.2 — Log level

| Level | Nghĩa | Ví dụ |
|---|---|---|
| `Trace` | Chi tiết tới mức lộ nội bộ | Giá trị biến trong vòng lặp |
| `Debug` | Thông tin chẩn đoán cho dev | Cache hit/miss, SQL sinh ra |
| `Information` | Sự kiện nghiệp vụ đáng ghi | Đơn hàng được tạo, người dùng đăng nhập |
| `Warning` | Bất thường nhưng đã xử lý được | Retry lần 2, cache không kết nối được nên đi thẳng DB |
| `Error` | Một thao tác thất bại | Không lưu được đơn hàng |
| `Critical` | Ứng dụng không phục vụ được | Mất kết nối database, sắp hết bộ nhớ |

Ba nhầm lẫn hay gặp:

- **`Error` cho mọi exception.** Người dùng nhập sai dữ liệu là chuyện bình thường, không phải sự cố. `OperationCanceledException` do client ngắt kết nối cũng vậy ([bài 6.8](../../stage-02-csharp-professional/module-06-async-programming/6.8-common-pitfalls.mdx)). Log mức `Error` cho chúng làm dashboard đầy báo động giả cho tới khi không ai nhìn nó nữa.
- **`Information` cho mọi thứ.** Log mức `Information` trong một vòng lặp 10.000 phần tử là chi phí thật và là nhiễu thật.
- **`Critical` không bao giờ dùng.** Nếu `Critical` không nối với cảnh báo gọi điện cho ai đó, nó chỉ là `Error` viết hoa.

Đặt mức theo namespace:

```json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning",
        "System.Net.Http.HttpClient": "Warning"
      }
    }
  }
}
```

Ba dòng `Override` này cắt phần lớn nhiễu: EF Core log **mọi** câu SQL ở mức `Information`, và `HttpClient` log mọi request đi ra.

### 8.8.3 — Cấu hình

```bash
dotnet add package Serilog.AspNetCore
dotnet add package Serilog.Sinks.Seq
```

```csharp
builder.Host.UseSerilog((ctx, services, config) => config
    .ReadFrom.Configuration(ctx.Configuration)   // doc tu appsettings.json
    .ReadFrom.Services(services)                 // enricher dang ky trong DI
    .Enrich.FromLogContext()                     // BAT BUOC cho scope
    .Enrich.WithMachineName()
    .Enrich.WithEnvironmentName());
```

Đặt cấu hình trong `appsettings.json` để đổi được theo môi trường mà không build lại:

```json
{
  "Serilog": {
    "Using": ["Serilog.Sinks.Console", "Serilog.Sinks.Seq"],
    "MinimumLevel": { "Default": "Information" },
    "WriteTo": [
      { "Name": "Console", "Args": { "formatter": "Serilog.Formatting.Compact.CompactJsonFormatter, Serilog.Formatting.Compact" } },
      { "Name": "Seq", "Args": { "serverUrl": "http://seq:5341" } }
    ]
  }
}
```

**Chọn sink theo nơi triển khai:**

- **Trong container** → chỉ ghi ra Console dạng JSON. Docker, Kubernetes và cloud đều thu thập stdout. Ghi ra file trong container là ghi vào một lớp sẽ biến mất, và nó còn làm image phình ra.
- **Ngoài container** → thêm `Serilog.Sinks.File` với `rollingInterval` và `retainedFileCountLimit`.
- **Tập trung** → Seq (nhẹ, dễ dựng), Elasticsearch, hoặc sink của nhà cung cấp cloud.

`CompactJsonFormatter` gọn hơn đáng kể so với `JsonFormatter` mặc định — đáng dùng khi log đi qua mạng.

### 8.8.4 — `UseSerilogRequestLogging`

```csharp
app.UseSerilogRequestLogging(options =>
{
    options.MessageTemplate =
        "HTTP {RequestMethod} {RequestPath} -> {StatusCode} trong {Elapsed:0.0}ms";

    options.GetLevel = (httpContext, elapsed, ex) =>
        ex is not null                          ? LogEventLevel.Error
        : httpContext.Response.StatusCode > 499 ? LogEventLevel.Error
        : elapsed > 3000                        ? LogEventLevel.Warning
        : httpContext.Request.Path.StartsWithSegments("/health")
                                                ? LogEventLevel.Verbose
                                                : LogEventLevel.Information;

    options.EnrichDiagnosticContext = (diag, http) =>
    {
        diag.Set("UserId", http.User.FindFirst("sub")?.Value);
        diag.Set("UserAgent", http.Request.Headers.UserAgent.ToString());
    };
});
```

Nó thay khoảng **mười dòng** log mà ASP.NET Core sinh ra cho mỗi request bằng **một dòng** có đủ method, path, status, thời gian và những gì bạn thêm vào.

`GetLevel` là phần đáng giá nhất: health check mỗi 10 giây sẽ chiếm phần lớn log của bạn nếu không hạ mức nó xuống, còn request chậm tự nổi lên `Warning` mà không cần middleware riêng.

Đặt nó **sớm** trong pipeline, ngay sau middleware xử lý lỗi và correlation ID ([bài 8.3](8.3-request-pipeline-and-middleware.mdx)).

### 8.8.5 — Scope

```csharp
using (logger.BeginScope(new Dictionary<string, object>
{
    ["OrderId"] = order.Id,
    ["TenantId"] = tenant.Id
}))
{
    logger.LogInformation("Bat dau xu ly");
    await ValidateAsync(order, ct);      // moi log ben trong deu co OrderId va TenantId
    await ChargeAsync(order, ct);
    logger.LogInformation("Hoan tat");
}
```

Scope gắn thuộc tính vào **mọi** log phát sinh bên trong nó, kể cả từ lớp khác ở sâu bên dưới. Đó là cách bạn lọc ra toàn bộ dấu vết của một đơn hàng qua nhiều lớp mà không phải truyền `orderId` vào từng lời gọi log.

Cần `Enrich.FromLogContext()` trong cấu hình, nếu không scope bị bỏ qua trong im lặng.

Serilog còn có `LogContext.PushProperty(...)` làm điều tương tự ở tầng Serilog — dùng nó trong middleware, như ở `CorrelationIdMiddleware`.

### 8.8.6 — Không bao giờ ghi vào log

Log thường được gửi sang hệ thống bên thứ ba, giữ nhiều tháng, và **nhiều người đọc được** hơn database. Không bao giờ ghi:

- Mật khẩu, kể cả mật khẩu sai người dùng vừa gõ.
- Token, API key, connection string.
- Số thẻ đầy đủ, CVV.
- Số CMND/CCCD, thông tin y tế.
- Toàn bộ request body của endpoint đăng nhập hoặc thanh toán.

```csharp
// SAI — @ ghi toan bo thuoc tinh, ke ca Password
logger.LogInformation("Dang nhap {@Request}", loginRequest);

// DUNG
logger.LogInformation("Dang nhap {Email}", loginRequest.Email);
```

Một tầng bảo vệ đáng thêm là destructuring policy, để cùng một quy tắc áp cho mọi chỗ:

```csharp
config.Destructure.ByTransforming<LoginRequest>(r => new { r.Email });
```

Và nhớ rằng URL cũng vào log: đừng đặt token trong query string.

### 8.8.7 — Rà lại code của bạn

<Checklist
  title="Danh sách rà soát logging"
  items={[
    { text: "Không còn lời gọi log nào dùng nội suy chuỗi với dấu $." },
    { text: "Placeholder đặt tên PascalCase và nhất quán giữa các lớp." },
    { text: "MinimumLevel có Override cho Microsoft.AspNetCore và EF Core Database.Command." },
    { text: "Lỗi do người dùng nhập sai không được log ở mức Error." },
    { text: "OperationCanceledException do client ngắt không log ở mức Error." },
    { text: "Trong container chỉ ghi Console dạng JSON, không ghi file." },
    { text: "UseSerilogRequestLogging được bật và hạ mức cho health check." },
    { text: "Enrich.FromLogContext() có trong cấu hình để scope hoạt động." },
    { text: "Không có mật khẩu, token hay số thẻ nào xuất hiện trong log." },
    { text: "Không dùng {@Object} cho model có chứa trường nhạy cảm." },
  ]}
/>

## Bài tập áp dụng

1. **Thấy khác biệt.** Ghi cùng một sự kiện bằng nội suy chuỗi và bằng message template, gửi vào Seq, rồi thử truy vấn theo `CustomerId`. Ghi lại cách nào tìm được.

2. **Đếm nhiễu.** Chạy ứng dụng 5 phút với health check mỗi 10 giây và **không** cấu hình `GetLevel`. Đếm số dòng log. Bật `GetLevel` hạ health check xuống `Verbose` và đếm lại.

3. **Rò rỉ qua `@`.** Log một `LoginRequest` bằng `{@Request}` và xác nhận mật khẩu xuất hiện trong output. Sửa bằng destructuring policy và kiểm chứng lại.

## Tự kiểm tra

<FAQSection
  items={[
    {
      question: "Vì sao không dùng nội suy chuỗi khi log?",
      answer: "Vì nó tạo ra một chuỗi đã nấu chín nên bạn chỉ tìm kiếm được bằng văn bản, không truy vấn được theo trường. Nó cũng luôn chạy kể cả khi mức log đó đang tắt, và làm mất khả năng gom nhóm các sự kiện cùng loại theo message template."
    },
    {
      question: "Tiền tố @ trong placeholder có tác dụng gì?",
      answer: "Nó bảo Serilog ghi cả đối tượng thành các thuộc tính thay vì gọi ToString. Nhưng nó ghi toàn bộ thuộc tính nên rất dễ đưa dữ liệu nhạy cảm như mật khẩu hay token vào log; với model có trường nhạy cảm thì nên dùng destructuring policy."
    },
    {
      question: "Những nhầm lẫn nào hay gặp với log level?",
      answer: "Ba cái. Dùng Error cho mọi exception, trong khi người dùng nhập sai hay client ngắt kết nối là chuyện bình thường, và làm vậy khiến dashboard đầy báo động giả. Dùng Information cho mọi thứ kể cả trong vòng lặp lớn. Và không bao giờ dùng Critical, trong khi nếu Critical không nối với cảnh báo thật thì nó chỉ là Error viết hoa."
    },
    {
      question: "Nên ghi log ra đâu khi chạy trong container?",
      answer: "Chỉ ghi ra Console dạng JSON, vì Docker, Kubernetes và cloud đều thu thập stdout. Ghi ra file trong container là ghi vào một lớp sẽ biến mất khi container bị thay, và nó còn làm image phình ra."
    },
    {
      question: "UseSerilogRequestLogging mang lại gì?",
      answer: "Nó thay khoảng mười dòng log mà ASP.NET Core sinh cho mỗi request bằng một dòng có đủ method, path, status và thời gian. Phần đáng giá nhất là GetLevel, cho phép hạ mức health check xuống rất thấp và tự nâng request chậm lên Warning mà không cần middleware riêng."
    },
    {
      question: "Scope trong logging để làm gì?",
      answer: "Nó gắn thuộc tính vào mọi log phát sinh bên trong nó, kể cả từ lớp khác ở sâu bên dưới, nên bạn lọc được toàn bộ dấu vết của một đơn hàng qua nhiều lớp mà không phải truyền id vào từng lời gọi log. Cần Enrich.FromLogContext trong cấu hình, nếu không scope bị bỏ qua trong im lặng."
    }
  ]}
/>

## Kết luận

Ba điều đáng nhớ nhất:

1. **Dấu `$` phá hỏng log.** Dùng message template với placeholder có tên.
2. **`Error` là cho sự cố**, không phải cho người dùng nhập sai hay client ngắt kết nối.
3. **Log được nhiều người đọc hơn database.** Không có mật khẩu, token hay số thẻ trong đó.

## Tham khảo

- [Logging in .NET Core and ASP.NET Core](https://learn.microsoft.com/en-us/aspnet/core/fundamentals/logging/)
- [High-performance logging in .NET](https://learn.microsoft.com/en-us/dotnet/core/extensions/high-performance-logging)
- [Serilog — structured logging for .NET](https://serilog.net/)
- [Serilog.AspNetCore on GitHub](https://github.com/serilog/serilog-aspnetcore)

## Điều hướng

- **Bài trước:** [8.7 — 6. Configuration System](8.7-configuration-system.mdx)
- **Bài tiếp theo:** [8.9 — 8. Global Exception Handling](8.9-global-exception-handling.mdx)
- **Về module:** [Trang mục lục](./)

## Bài liên quan

- [Vì sao EF Core bắn 201 query cho 1 màn hình danh sách? Cách sửa N+1](/blog/ef-core-n-plus-1-query) — Một màn hình 200 dòng mà log SQL ghi 201 câu lệnh là dấu hiệu của N+1 query.
