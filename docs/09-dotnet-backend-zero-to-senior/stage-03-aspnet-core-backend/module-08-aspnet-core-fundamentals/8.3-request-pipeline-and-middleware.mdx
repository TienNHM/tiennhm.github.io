---
title: "8.3 — 2. Request Pipeline và Middleware"
sidebar_position: 3
description: "Request Pipeline và Middleware trong ASP.NET Core: cách middleware xếp chuỗi, short-circuit, thứ tự đăng ký và correlation/timing cho CRM backend."
tags:
  - aspnet-core
  - middleware
  - request-pipeline
keywords:
  - ASP.NET Core middleware
  - request pipeline
  - correlation id
  - timing middleware
  - CRM backend
enableComments: true
draft: false
---

import { SummaryBox, HiddenContent } from '@site/src/components/SEO';

<SummaryBox title="Tóm tắt (abstract)">
Request Pipeline là “đường đi” của mỗi HTTP request qua ASP.NET Core: từ routing → middleware chain → handler, rồi ngược lại cho response.
Trong CRM, pipeline giúp bạn chuẩn hóa logging (CorrelationId), đo latency và xử lý lỗi một cách nhất quán.
</SummaryBox>
<HiddenContent>
<!-- geoseo-curriculum-nav -->
## Liên kết điều phối (curriculum graph)
- **Hub lộ trình**: [00. Lộ trình From Zero → Senior .NET (Backend-first)](../../roadmap-dotnet-backend-zero-to-senior.mdx)
- **Bài trước**: [8.2 — 1. Kiến trúc ASP.NET Core](8.2-aspnet-core-architecture.mdx)
- **Bài tiếp theo**: [8.4 — 3. Routing](8.4-routing.mdx)

## Tham chiếu có thẩm quyền (E-E-A-T / retrieval)
- [Middleware in ASP.NET Core](https://learn.microsoft.com/en-us/aspnet/core/fundamentals/middleware/)
- [Routing in ASP.NET Core](https://learn.microsoft.com/en-us/aspnet/core/fundamentals/routing)
</HiddenContent>

# 8.3 — 2. Request Pipeline và Middleware

## Mục tiêu bài học

- Nắm được ý chính của bài và mối liên hệ với module.
- Áp dụng được kiến thức vào bối cảnh CRM/.NET backend.
- Sẵn sàng chuyển sang bài kế tiếp với nền tảng chắc chắn.

## Nội dung bài học

### 8.3.1 — Cách pipeline hoạt động

Request HTTP đi qua một chuỗi middleware theo thứ tự đăng ký, mỗi middleware có thể:
- Xử lý request rồi **chuyển tiếp** (`await next(context)`)
- **Dừng** pipeline (short-circuit) và trả response ngay

```
Request → [MW1] → [MW2] → [MW3] → Handler
Response ← [MW1] ← [MW2] ← [MW3] ←
```

### 8.3.2 — Use, Run, Map

```csharp
// Use — xử lý rồi chuyển tiếp
app.Use(async (context, next) =>
{
    // Trước khi xử lý
    await next(context);
    // Sau khi xử lý
});

// Run — terminal middleware, KHÔNG gọi next
app.Run(async context =>
{
    await context.Response.WriteAsync("Hello from terminal middleware");
});

// Map — rẽ nhánh theo path
app.Map("/health", branch =>
{
    branch.Run(async context =>
        await context.Response.WriteAsync("Healthy"));
});
```

### 8.3.3 — Viết Custom Middleware — Request Timing

```csharp
// Middleware/RequestTimingMiddleware.cs
public class RequestTimingMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ILogger<RequestTimingMiddleware> _logger;

    public RequestTimingMiddleware(RequestDelegate next,
        ILogger<RequestTimingMiddleware> logger)
    {
        _next = next;
        _logger = logger;
    }

    public async Task InvokeAsync(HttpContext context)
    {
        var stopwatch = Stopwatch.StartNew();
        try
        {
            await _next(context);
        }
        finally
        {
            stopwatch.Stop();
            _logger.LogInformation(
                "HTTP {Method} {Path} responded {StatusCode} in {Elapsed}ms",
                context.Request.Method,
                context.Request.Path,
                context.Response.StatusCode,
                stopwatch.ElapsedMilliseconds);
        }
    }
}

// Extension method để đăng ký
public static class RequestTimingMiddlewareExtensions
{
    public static IApplicationBuilder UseRequestTiming(
        this IApplicationBuilder builder)
        => builder.UseMiddleware<RequestTimingMiddleware>();
}
```

### 8.3.4 — Viết Custom Middleware — Correlation ID

```csharp
// Middleware/CorrelationIdMiddleware.cs
public class CorrelationIdMiddleware
{
    private const string CorrelationIdHeader = "X-Correlation-ID";
    private readonly RequestDelegate _next;

    public CorrelationIdMiddleware(RequestDelegate next) => _next = next;

    public async Task InvokeAsync(HttpContext context)
    {
        var correlationId = context.Request.Headers[CorrelationIdHeader].FirstOrDefault()
            ?? Guid.NewGuid().ToString();

        context.Items["CorrelationId"] = correlationId;
        context.Response.Headers[CorrelationIdHeader] = correlationId;

        // Đẩy vào LogContext để mọi log trong request đều có correlation ID
        using (LogContext.PushProperty("CorrelationId", correlationId))
        {
            await _next(context);
        }
    }
}
```

### 8.3.5 — Thứ tự middleware quan trọng

```csharp
// THỨ TỰ ĐÚNG trong Program.cs
app.UseExceptionHandler("/error"); // Phải đứng ĐẦU để bắt mọi exception
app.UseMiddleware<CorrelationIdMiddleware>(); // Correlation ID sớm để log được
app.UseRequestTiming();             // Timing sau correlation
app.UseHttpsRedirection();
app.UseRouting();
app.UseAuthentication();            // Authn trước Authz
app.UseAuthorization();
app.MapControllers();
```

## Common pitfalls
- Đặt `UseExceptionHandler` không ở đầu pipeline → exception có thể lọt ra ngoài và bạn mất tính nhất quán cho response lỗi.
- Gắn correlation/timing middleware quá muộn → log sớm trong pipeline không có `X-Correlation-ID`.
- Nhầm thứ tự `UseAuthentication`/`UseAuthorization` → principal/claims không sẵn sàng, dẫn tới thiếu quyền.
- Quên `await next(context)` hoặc trả response sớm ở middleware chain không đúng mục tiêu → handler có thể chạy thiếu bước.
- Middleware thiết kế không idempotent (có side effects) → chạy lại/đổi thứ tự dễ gây trạng thái sai khó debug.

## Ví dụ trong CRM/.NET backend
- Khi request vào `POST /api/v1/customers`, bạn dùng `CorrelationId` để join log API với log service nền/worker để điều tra lỗi theo “customer create bị lỗi ở phút nào”.
- Khi audit, timing middleware giúp bạn tách latency do authentication, do controller hay do truy vấn SQL (trước khi tối ưu query).

---

## Bài tập áp dụng

1. Tóm tắt bài học bằng ngôn ngữ của bạn.
2. Liên hệ nội dung với một tình huống thực tế trong dự án.
3. Đề xuất một cải tiến cụ thể sau khi học bài này.

## Tự kiểm tra

- Bạn có thể giải thích lại nội dung chính trong 2 phút không?
- Bạn có ví dụ áp dụng thực tế chưa?
- Bạn biết bước tiếp theo cần học/triển khai là gì không?

## Kết luận

Hoàn thành bài này giúp bạn có góc nhìn đầy đủ hơn trước khi đi tiếp trong module.

## Điều hướng

- **Bài trước:** [8.2 — 1. Kiến trúc ASP.NET Core](8.2-aspnet-core-architecture.mdx)
- **Bài tiếp theo:** [8.4 — 3. Routing](8.4-routing.mdx)
- **Về module:** [Trang mục lục](index.mdx)
