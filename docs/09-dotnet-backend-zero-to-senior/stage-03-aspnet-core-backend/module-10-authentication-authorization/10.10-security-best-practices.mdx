---
title: "10.10 — 10. Security Best Practices"
description: "Danh sách rà soát bảo mật cho API .NET: bí mật, HTTPS và HSTS, security header, CORS, audit log, và những gì không bao giờ được ghi hay trả về."
sidebar_position: 10
---

import { SummaryBox, Checklist, FAQSection } from '@site/src/components/SEO';

# 10.11 — 10. Security Best Practices

<SummaryBox>
Bài này gom những việc **luôn đúng** cho mọi API .NET, phần lớn tốn vài dòng cấu hình nhưng bịt được những lỗ hổng bị khai thác nhiều nhất. Ba thứ có tác động lớn nhất so với công sức bỏ ra: **`CORS` với `AllowAnyOrigin` cộng `AllowCredentials` không chạy được** — và cách người ta "sửa" nó (phản chiếu lại origin của request) chính là **tắt hoàn toàn** cơ chế bảo vệ same-origin; **secret trong Git không xoá được bằng commit mới**, phải coi như đã lộ và xoay khoá; và **log là nơi rò rỉ dữ liệu phổ biến nhất** vì nó được giữ lâu hơn và nhiều người đọc được hơn database.
</SummaryBox>

## Mục tiêu bài học

Sau bài này bạn có thể:

- Xử lý đúng khi một secret lọt vào Git.
- Cấu hình HTTPS, HSTS và security header.
- Cấu hình CORS không mở toang.
- Thiết kế audit log dùng được khi điều tra sự cố.
- Rà một API trước khi đưa lên production.

## Nội dung bài học

### 10.11.1 — Bí mật

```csharp
// SAI — nam trong Git, trong image Docker, trong moi ban sao repo
"Jwt": { "SecretKey": "super-secret-key-123456789012345678901234" }
```

`appsettings.json` **nằm trong Git**. Mọi thứ trong đó đều công khai với mọi người từng clone repo, và với mọi lớp trong image Docker.

**Khi một secret đã bị commit:**

1. **Xoay khoá ngay.** Đây là bước duy nhất thực sự có tác dụng.
2. Xoá khỏi lịch sử (`git filter-repo`) — chỉ để dọn dẹp, **không** để khắc phục.
3. Coi như nó **đã bị lộ**: repo có thể đã được fork, clone, hoặc có bản sao trong CI cache.

Một commit "xoá secret" không xoá gì cả — nó vẫn nằm trong lịch sử và trong mọi bản sao.

```bash
# Development
dotnet user-secrets set "Jwt:SecretKey" "$(openssl rand -base64 48)"
```

```yaml
# Production — tu secret cua orchestrator
env:
  - name: Jwt__SecretKey
    valueFrom:
      secretKeyRef: { name: crm-secrets, key: jwt-key }
```

Thêm một tầng chặn trong CI để không tái diễn:

```yaml
- name: Scan for secrets
  uses: gitleaks/gitleaks-action@v2
```

Và **kiểm tra lúc khởi động** rằng secret hợp lệ:

```csharp
builder.Services.AddOptions<JwtOptions>()
    .Bind(builder.Configuration.GetSection("Jwt"))
    .Validate(o => Encoding.UTF8.GetByteCount(o.SecretKey) >= 32,
              "Jwt:SecretKey phải tối thiểu 32 byte")
    .Validate(o => !o.SecretKey.Contains("change-me"),
              "Jwt:SecretKey vẫn là giá trị mẫu")
    .ValidateOnStart();
```

`ValidateOnStart()` ([bài 7.8](../../stage-02-csharp-professional/module-07-dependency-injection/7.7-options-pattern.mdx)) khiến ứng dụng **không khởi động được** với cấu hình sai — tốt hơn nhiều so với chạy bình thường bằng khoá mẫu.

### 10.11.2 — HTTPS và HSTS

```csharp
if (!app.Environment.IsDevelopment())
{
    app.UseHsts();
    app.UseHttpsRedirection();
}
```

```csharp
builder.Services.AddHsts(options =>
{
    options.MaxAge = TimeSpan.FromDays(365);
    options.IncludeSubDomains = true;
    options.Preload = true;
});
```

`UseHttpsRedirection` chỉ chuyển hướng **sau khi** request HTTP đầu tiên đã đi qua mạng — và request đó có thể đã mang theo cookie. HSTS bảo trình duyệt **không bao giờ** thử HTTP với tên miền này nữa, kể từ lần thứ hai.

Hai cảnh báo:

- **`Preload` là một chiều.** Đưa tên miền vào danh sách preload của trình duyệt thì gỡ ra mất **nhiều tháng**. Chỉ bật khi chắc chắn mọi subdomain đều có HTTPS.
- **`IncludeSubDomains` áp cho mọi subdomain**, kể cả những cái bạn quên. Một `legacy.company.com` chỉ có HTTP sẽ ngừng truy cập được.

Sau reverse proxy, phải bật `UseForwardedHeaders()` **trước** ([bài 8.2](../module-08-aspnet-core-fundamentals/8.1-aspnet-core-architecture.mdx)) — nếu không, ứng dụng thấy mọi request là HTTP và `UseHttpsRedirection` tạo **vòng lặp chuyển hướng vô tận**.

### 10.11.3 — Security header

```csharp
app.Use(async (context, next) =>
{
    var headers = context.Response.Headers;

    headers["X-Content-Type-Options"] = "nosniff";
    headers["X-Frame-Options"] = "DENY";
    headers["Referrer-Policy"] = "no-referrer";
    headers["Content-Security-Policy"] = "default-src 'none'; frame-ancestors 'none'";
    headers["Permissions-Policy"] = "geolocation=(), microphone=(), camera=()";

    headers.Remove("Server");
    headers.Remove("X-Powered-By");

    await next(context);
});
```

| Header | Chống |
|---|---|
| `X-Content-Type-Options: nosniff` | Trình duyệt đoán kiểu nội dung và thực thi JSON như JavaScript |
| `X-Frame-Options: DENY` | Clickjacking |
| `Referrer-Policy: no-referrer` | Rò rỉ URL (có thể chứa id, token) sang site khác |
| `Content-Security-Policy` | XSS — với API JSON thì `default-src 'none'` là đúng |
| `Permissions-Policy` | Truy cập thiết bị ngoài ý muốn |

Với **API JSON thuần**, CSP `default-src 'none'` là chính xác: API không bao giờ nên tải tài nguyên nào. Nếu bạn phục vụ cả Swagger UI thì cần CSP riêng cho đường dẫn đó.

Gỡ `Server` giảm thông tin trinh sát. Kestrel gỡ được tại nguồn:

```csharp
builder.WebHost.ConfigureKestrel(o => o.AddServerHeader = false);
```

### 10.11.4 — CORS

```csharp
// SAI — không thể chạy, và cách "sửa" nó còn tệ hơn
policy.AllowAnyOrigin().AllowCredentials();   // nem exception luc khoi dong
```

Trình duyệt **cấm** kết hợp `Access-Control-Allow-Origin: *` với `Access-Control-Allow-Credentials: true`, và ASP.NET Core ném exception ngay lúc khởi động.

Cách "sửa" mà nhiều người tìm thấy trên mạng là **phản chiếu** origin của request:

```csharp
// RẤT NGUY HIỂM — chấp nhận MỌI origin
policy.SetIsOriginAllowed(_ => true).AllowCredentials();
```

Nó "hoạt động" — và nó **tắt hoàn toàn** cơ chế bảo vệ same-origin. Bất kỳ trang web nào cũng gọi được API của bạn **kèm cookie của người dùng**.

```csharp
// ĐÚNG — danh sách trắng, đọc từ cấu hình
builder.Services.AddCors(options =>
{
    options.AddPolicy("app", policy => policy
        .WithOrigins(builder.Configuration.GetSection("Cors:Origins").Get<string[]>()!)
        .WithMethods("GET", "POST", "PUT", "PATCH", "DELETE")
        .WithHeaders("Authorization", "Content-Type")
        .AllowCredentials()
        .SetPreflightMaxAge(TimeSpan.FromMinutes(10)));
});

app.UseCors("app");     // TRƯỚC UseAuthorization
```

Nhớ cái bẫy mảng cấu hình ở [bài 8.7](../module-08-aspnet-core-fundamentals/8.6-configuration-system.mdx): khai `Cors:Origins` ở cả `appsettings.json` lẫn file môi trường sẽ **hợp nhất theo chỉ số**, nên một origin lẽ ra đã bị xoá vẫn được chấp nhận trên production.

Nếu API chỉ được gọi từ server khác (không từ trình duyệt), **đừng bật CORS**. Nó không bảo vệ gì cả — nó chỉ nới lỏng một hạn chế của trình duyệt.

### 10.11.5 — Không bao giờ ghi hay trả về

```csharp
// SAI
logger.LogInformation("Dang nhap {Email} voi mat khau {Password}", email, password);
logger.LogDebug("Token: {Token}", accessToken);
return Problem(detail: exception.ToString());
```

| Không bao giờ vào log | Không bao giờ vào response |
|---|---|
| Mật khẩu, kể cả mật khẩu sai | Stack trace |
| Token, API key, connection string | Tên bảng, tên cột |
| Số thẻ đầy đủ, CVV | Phiên bản thư viện |
| Toàn bộ body của endpoint đăng nhập | Thông điệp exception của database |
| Số CMND/CCCD, hồ sơ y tế | Đường dẫn file trên server |

Log được giữ **hàng tháng**, gửi sang hệ thống bên thứ ba, và **nhiều người đọc được hơn** database. Rất nhiều sự cố rò rỉ dữ liệu bắt đầu từ log, không phải từ database.

Cẩn thận với destructuring: `logger.LogInformation("{@Request}", loginRequest)` ghi **toàn bộ** thuộc tính, gồm cả mật khẩu ([bài 8.8](../module-08-aspnet-core-fundamentals/8.7-logging-with-serilog.mdx)).

Và URL cũng vào log — đừng bao giờ đặt token trong query string.

### 10.11.6 — Audit log

Audit log khác log kỹ thuật: nó trả lời **"ai đã làm gì, lúc nào"** và phải dùng được khi điều tra.

```csharp
public sealed class AuditEntry
{
    public Guid Id { get; init; } = Guid.NewGuid();
    public DateTime OccurredAt { get; init; } = DateTime.UtcNow;
    public required string ActorId { get; init; }        // AI
    public required string Action { get; init; }         // LAM GI
    public string? TargetType { get; init; }             // TREN CAI GI
    public string? TargetId { get; init; }
    public string? IpAddress { get; init; }
    public string? UserAgent { get; init; }
    public string? Changes { get; init; }                // JSON trước/sau
    public required bool Succeeded { get; init; }
}
```

Những sự kiện **phải** có:

- Đăng nhập thành công và thất bại
- Đăng xuất, đăng xuất tất cả thiết bị
- Đổi mật khẩu, đặt lại mật khẩu
- **Gán và gỡ vai trò, cấp và gỡ quyền**
- Xoá hoặc sửa dữ liệu nhạy cảm
- Xuất dữ liệu hàng loạt
- Phát hiện tái sử dụng refresh token ([bài 10.4](10.3-refresh-token.mdx))

Bốn nguyên tắc:

1. **Chỉ ghi thêm.** Không `UPDATE`, không `DELETE`. Kẻ tấn công vào được hệ thống không được xoá dấu vết.
2. **Ghi cả thất bại.** Chuỗi 500 lần đăng nhập thất bại là thông tin quan trọng hơn một lần thành công.
3. **Tách khỏi log kỹ thuật.** Chúng có vòng đời, quyền truy cập và yêu cầu lưu trữ khác nhau.
4. **Không chứa dữ liệu nhạy cảm.** Ghi "đã đổi mật khẩu", không ghi mật khẩu.

Điểm 1 thường bị bỏ qua: nếu audit log nằm cùng database và cùng quyền với dữ liệu nghiệp vụ, nó không bảo vệ được gì trước người có quyền admin.

### 10.11.7 — Rà lại trước khi lên production

<Checklist
  title="Danh sách rà soát bảo mật"
  items={[
    { text: "Không có secret nào trong Git; CI có bước quét secret." },
    { text: "Secret bị lộ đã được xoay khoá, không chỉ xoá khỏi file." },
    { text: "Cấu hình bảo mật được validate lúc khởi động với ValidateOnStart." },
    { text: "HSTS bật ở production, hiểu rõ Preload là một chiều." },
    { text: "UseForwardedHeaders đặt trước UseHttpsRedirection." },
    { text: "Security header đầy đủ; header Server đã gỡ." },
    { text: "CORS dùng danh sách trắng, không phản chiếu origin của request." },
    { text: "Mảng origin trong cấu hình chỉ khai ở một nơi." },
    { text: "Không có mật khẩu, token hay số thẻ nào trong log." },
    { text: "Không trả stack trace hay thông điệp exception ra client ở production." },
    { text: "Audit log chỉ ghi thêm, ghi cả thất bại, tách khỏi log kỹ thuật." },
    { text: "Endpoint đăng nhập và quên mật khẩu có rate limit." },
    { text: "FallbackPolicy yêu cầu đăng nhập được đặt." },
    { text: "Mọi endpoint nhận id đều kiểm tra quyền trên bản ghi." },
    { text: "Swagger UI không mở công khai ở production." },
    { text: "Phụ thuộc được quét lỗ hổng định kỳ (dotnet list package --vulnerable)." },
  ]}
/>

## Bài tập áp dụng

1. **CORS phản chiếu.** Cấu hình `SetIsOriginAllowed(_ => true)` với `AllowCredentials`, rồi từ một trang HTML ở origin khác gọi API kèm cookie. Xác nhận nó thành công — và hiểu vì sao đó là lỗ hổng.

2. **Quét lỗ hổng.** Chạy `dotnet list package --vulnerable --include-transitive` trên một dự án thật. Ghi lại số gói có lỗ hổng đã biết, kể cả phụ thuộc gián tiếp.

3. **Secret trong lịch sử.** Commit một secret giả, rồi commit tiếp để "xoá" nó. Dùng `git log -p` tìm lại giá trị cũ và xác nhận nó vẫn còn nguyên.

## Tự kiểm tra

<FAQSection
  items={[
    {
      question: "Khi một secret đã bị commit vào Git thì làm gì?",
      answer: "Xoay khoá ngay, đó là bước duy nhất thực sự có tác dụng. Xoá khỏi lịch sử chỉ để dọn dẹp chứ không khắc phục, vì repo có thể đã được fork, clone hoặc có bản sao trong CI cache. Một commit xoá secret không xoá gì cả, giá trị cũ vẫn nằm trong lịch sử."
    },
    {
      question: "HSTS giải quyết gì mà UseHttpsRedirection không giải quyết được?",
      answer: "UseHttpsRedirection chỉ chuyển hướng sau khi request HTTP đầu tiên đã đi qua mạng, và request đó có thể đã mang theo cookie. HSTS bảo trình duyệt không bao giờ thử HTTP với tên miền này nữa kể từ lần thứ hai. Nhưng Preload là một chiều và IncludeSubDomains áp cho cả subdomain bạn quên."
    },
    {
      question: "Vì sao AllowAnyOrigin không kết hợp được với AllowCredentials?",
      answer: "Vì trình duyệt cấm kết hợp Access-Control-Allow-Origin dấu sao với Allow-Credentials true, và ASP.NET Core ném exception ngay lúc khởi động. Cách sửa mà nhiều người tìm thấy là phản chiếu origin của request, nhưng nó tắt hoàn toàn cơ chế bảo vệ same-origin và cho bất kỳ trang web nào gọi API kèm cookie của người dùng."
    },
    {
      question: "API chỉ gọi từ server khác thì có cần CORS không?",
      answer: "Không. CORS là một hạn chế của trình duyệt, nên nó chỉ có ý nghĩa khi API được gọi từ JavaScript trong trình duyệt. Bật CORS cho API gọi từ server không bảo vệ gì cả, nó chỉ nới lỏng một hạn chế không liên quan."
    },
    {
      question: "Vì sao log là nơi rò rỉ dữ liệu phổ biến?",
      answer: "Vì log được giữ hàng tháng, gửi sang hệ thống bên thứ ba, và nhiều người đọc được hơn database. Rất nhiều sự cố rò rỉ bắt đầu từ log chứ không phải từ database. Cần cẩn thận cả với destructuring vì nó ghi toàn bộ thuộc tính của object, gồm cả mật khẩu."
    },
    {
      question: "Audit log cần những nguyên tắc nào?",
      answer: "Chỉ ghi thêm, không cập nhật và không xoá, để kẻ tấn công vào được hệ thống cũng không xoá được dấu vết. Ghi cả các lần thất bại, vì chuỗi 500 lần đăng nhập thất bại quan trọng hơn một lần thành công. Tách khỏi log kỹ thuật vì khác vòng đời và quyền truy cập. Và không chứa dữ liệu nhạy cảm."
    }
  ]}
/>

## Kết luận

Ba điều đáng nhớ nhất:

1. **Secret đã commit là secret đã lộ.** Xoay khoá, đừng chỉ xoá file.
2. **Phản chiếu origin trong CORS là tắt bảo vệ same-origin**, không phải sửa lỗi.
3. **Audit log chỉ ghi thêm và ghi cả thất bại** — nếu không, nó vô dụng đúng lúc bạn cần nhất.

## Tham khảo

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Enforce HTTPS in ASP.NET Core](https://learn.microsoft.com/en-us/aspnet/core/security/enforcing-ssl)
- [Enable CORS in ASP.NET Core](https://learn.microsoft.com/en-us/aspnet/core/security/cors)
- [ASP.NET Core Data Protection](https://learn.microsoft.com/en-us/aspnet/core/security/data-protection/introduction)

## Điều hướng

- **Bài trước:** [10.9 — 9. External Login và OAuth2 (Overview)](10.9-overview.mdx)
- **Bài tiếp theo:** [10.11 — Mở rộng và đào sâu](10.11-advanced-notes.mdx)
- **Về module:** [Trang mục lục](./)
