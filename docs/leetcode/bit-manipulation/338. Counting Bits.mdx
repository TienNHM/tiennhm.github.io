---
title: 338. Counting Bits
slug: 338-counting-bits
description: Given an integer `n`, return an array `ans` of length `n + 1` such that for each `i` (0 <= i <= n), `ans[i]` is the number of 1's in the binary representation of `i`.
image: https://leetcode.com/static/images/LeetCode_Sharing.png
sidebar_position: 1
tags:
    - leetcode
    - bit-manipulation
keywords: [leetcode, bit-manipulation]
enableComments: true # for Gisqus comments, set to true
draft: false # set to true to hide this post from the site
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';
import CountingBitsCs from '!!raw-loader!./csharp/338-counting-bits.cs';
import CountingBitsPy from '!!raw-loader!./python/338-counting-bits.py';
import CountingBitsJs from '!!raw-loader!./javascript/338-counting-bits.js';
import CountingBitsTs from '!!raw-loader!./typescript/338-counting-bits.ts';

# 338. Counting Bits

## Giới thiệu bài toán

Chi tiết: https://leetcode.com/problems/counting-bits/

:::tip Yêu cầu
Given an integer `n`, return an array `ans` of length `n + 1` such that for each `i` (0 <= i <= n), `ans[i]` is the number of 1's in the binary representation of `i`.
:::

**Example 1:**

- Input: n = 2
- Output: [0,1,1]
- Explanation:
```log
0 --> 0
1 --> 1
2 --> 10
```

**Example 2:**

- Input: n = 5
- Output: [0,1,1,2,1,2]
- Explanation:
```log
0 --> 0
1 --> 1
2 --> 10
3 --> 11
4 --> 100
5 --> 101
```

**Constraints:**

> 0 <= `n` <= 105
 
**Follow up:**

- It is very easy to come up with a solution with a runtime of `O(n log n)`. Can you do it in linear time `O(n)` and possibly in a single pass?
- Can you do it without using any built-in function (i.e., like `__builtin_popcount` in C++)?

## Giải quyết bài toán

<Tabs defaultValue="csharp" groupId="language" queryString>
  <TabItem value="csharp" label="C#">
    <CodeBlock language="csharp" showLineNumbers>
        {CountingBitsCs}
    </CodeBlock>
  </TabItem>
  <TabItem value="python" label="Python">
    <CodeBlock language="python" showLineNumbers>
        {CountingBitsPy}
    </CodeBlock>
  </TabItem>
  <TabItem value="javascript" label="JavaScript">
    <CodeBlock language="javascript" showLineNumbers>
        {CountingBitsJs}
    </CodeBlock>
  </TabItem>
   <TabItem value="typescript" label="TypeScript">
    <CodeBlock language="typescript" showLineNumbers>
        {CountingBitsTs}
    </CodeBlock>
  </TabItem>
</Tabs>

**Tham khảo:**
- [C#](https://leetcode.com/submissions/detail/1039509439/)
- [Python](https://leetcode.com/submissions/detail/1039538497/)
- [JavaScript](https://leetcode.com/submissions/detail/1039545417/)
- [TypeScript](https://leetcode.com/submissions/detail/1039547895/)